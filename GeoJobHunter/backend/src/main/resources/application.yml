server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/jobhun
    username: core
    password: arie12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
