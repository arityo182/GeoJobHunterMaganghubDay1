# ============================================================
# JANGAN set VITE_API_BASE_URL di production (Vercel)!
# Vercel rewrite di vercel.json otomatis proxy /api/* ke Railway
# Biarkan fallback ke '/api' supaya same-origin (no CORS issues)
# ============================================================
# Development (local backend):
# VITE_API_BASE_URL=http://localhost:8080

# Map defaults
VITE_MAP_DEFAULT_LAT=-6.2000
VITE_MAP_DEFAULT_LON=106.8166
VITE_MAP_DEFAULT_ZOOM=12
